# Peka på en const och en let — varför just den typen där?
const isMember = true är en const eftersom värdet inte ska ändras.
let shipping = 79 är en let eftersom värdet kan ändras och i koden ändras det till 0 om person är medlem.

## Peka på ett anrop med () — vad returneras?
console.log(totalMinutes(2, 15)); är ett anrop med(). funktion returnerar 135 föratt 2*60 +15 = 135.

### Peka på ett === — vad jämförs?

if (isMember === true) använder ===. det jämför värdet och typen och i koden jämförs isMember med true

Kodförståelse

# Vad är skillnaden mellan deklaration och anrop av en function?
en decleration skapar funktionen och beskriver vad den ska göra. ett anrop med () kör funktionen.

## Varför börjar array-index på 0?
det första elementet i en arry har index 0, därför börjar indexering på 0.

###  Vad betyder undefined när du läser cities[99]?
det betyder att det finns inte något värde på index 99 i arrayen.Det finns inget index 99 i arrayen.